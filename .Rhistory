rm(list=ls()) ## just clears everything down to start
# Needed <- c("tm", "SnowballC", "stringr")            ## These lines make sure that the packages
# install.packages(Needed, dependencies=TRUE)   ## are installed
library(tm)
library(SnowballC)
library(stringr)
tfile=read.csv(file.choose())  ## opens a file browser so can use other files than your fixed one
tfile=tfile[1] ## only want first column
View(tfile)
View(tfile)
tweet_corpus <- Corpus(VectorSource(tfile))
myCorpus <- tweet_corpus
removeURL <- function(x) gsub("http[[:alnum:][:punct:]]*", " ", x)
myCorpus <- tm_map(myCorpus, removeURL)
removeHandle <- function(x) gsub("@[[:alnum:][:punct:]]*"," ",x)
myCorpus <- tm_map(myCorpus, removeHandle)
removeHash <- function(x) gsub("#[[:alnum:][:punct:]]*"," ",x)
myCorpus <- tm_map(myCorpus, removeHash)
inspect(myCorpus)
myCorpus <- tweet_corpus
removeURL <- function(x) gsub("http[[:alnum:][:punct:]]*", " ", x)
myCorpus <- tm_map(myCorpus, removeURL)
removeHandle <- function(x) gsub("@[[:alnum:][:punct:]]*"," ",x)
myCorpus <- tm_map(myCorpus, removeHandle)
removeHash <- function(x) gsub("#[[:alnum:][:punct:]]*"," ",x)
myCorpus <- tm_map(myCorpus, removeHash)
changeAmp <- function(x) gsub("&amp;","&",x)
inspect(myCorpus)
myCorpus <- tm_map(myCorpus, changeAmp)
inspect(myCorpus)
rm(list=ls()) ## just clears everything down to start
# Needed <- c("tm", "SnowballC", "stringr")            ## These lines make sure that the packages
# install.packages(Needed, dependencies=TRUE)   ## are installed
library(tm)
library(SnowballC)
library(stringr)
tfile=read.csv(file.choose())  ## opens a file browser so can use other files than your fixed one
tfile=tfile[1] ## only want first column
tweet_corpus <- Corpus(VectorSource(tfile))
myCorpus <- tweet_corpus
removeURL <- function(x) gsub("http[[:alnum:][:punct:]]*", " ", x)
myCorpus <- tm_map(myCorpus, removeURL)
removeHandle <- function(x) gsub("@[[:alnum:][:punct:]]*"," ",x)
myCorpus <- tm_map(myCorpus, removeHandle)
removeHash <- function(x) gsub("#[[:alnum:][:punct:]]*"," ",x)
myCorpus <- tm_map(myCorpus, removeHash)
changeAmp <- function(x) gsub("&amp;"," ",x)
myCorpus <- tm_map(myCorpus, changeAmp)
rm(list=ls()) ## just clears everything down to start
# setwd <- "wherever you store the datafile" ## not really necessary, but makes things easier
# Needed <- c("tm", "SnowballC", "stringr")            ## These lines make sure that the packages
# install.packages(Needed, dependencies=TRUE)   ## are installed
library(tm)
#library(SnowballC)
#library(stringr)
tfile=read.csv(file.choose())  ## opens a file browser so can use other files than your fixed one
tfile=tfile[1] ## only want first column
tweet_corpus <- Corpus(VectorSource(tfile))
myCorpus <- tweet_corpus
removeURL <- function(x) gsub("http[[:alnum:][:punct:]]*", " ", x)
myCorpus <- tm_map(myCorpus, removeURL)
removeHandle <- function(x) gsub("@[[:alnum:][:punct:]]*"," ",x)
myCorpus <- tm_map(myCorpus, removeHandle)
removeHash <- function(x) gsub("#[[:alnum:][:punct:]]*"," ",x)
myCorpus <- tm_map(myCorpus, removeHash)
changeAmp <- function(x) gsub("&amp;"," ",x)
myCorpus <- tm_map(myCorpus, changeAmp)
myCorpus <- tm_map(myCorpus, tolower)
rm(list=ls()) ## just clears everything down to start
setwd(d:/Data/github/Grant) <- "wherever you store the datafile" ## not really necessary, but makes things easier
# Needed <- c("tm", "SnowballC", "stringr")            ## These lines make sure that the packages
# install.packages(Needed, dependencies=TRUE)   ## are installed
library(tm)
#library(SnowballC)
#library(stringr)
rm(list=ls()) ## just clears everything down to start
setwd(d:/Data/github/Grant) ## sets working directory, not really necessary, but makes things easier
getwd()
rm(list=ls()) ## just clears everything down to start
setwd("d:/Data/github/Grant") ## sets working directory, not really necessary, but makes things easier
getwd()
rm(list=ls()) ## just clears everything down to start
setwd("d:/Data/github/Grant") ## sets working directory, not really necessary, but makes things easier
# Needed <- c("tm", "SnowballC", "stringr")            ## These lines make sure that the packages
# install.packages(Needed, dependencies=TRUE)   ## are installed
library(tm)
#library(SnowballC)
#library(stringr)
tfile=read.csv(file.choose())  ## opens a file browser so can use other files than your fixed one
tfile=tfile[1] ## only want first column
tweet_corpus <- Corpus(VectorSource(tfile))
myCorpus <- tweet_corpus
removeURL <- function(x) gsub("http[[:alnum:][:punct:]]*", " ", x)
myCorpus <- tm_map(myCorpus, removeURL)
removeHandle <- function(x) gsub("@[[:alnum:][:punct:]]*"," ",x)
myCorpus <- tm_map(myCorpus, removeHandle)
removeHash <- function(x) gsub("#[[:alnum:][:punct:]]*"," ",x)
myCorpus <- tm_map(myCorpus, removeHash)
changeAmp <- function(x) gsub("&amp;"," ",x)
myCorpus <- tm_map(myCorpus, changeAmp)
inspect(myCorpus)
replaceApostrophe <- function(x) gsub("‰Ûª","'",x) ‰Ûª
myCorpus <- tm_map(myCorpus, replaceApostrophe)
replaceApostrophe <- function(x) gsub("‰Ûª","'",x)
myCorpus <- tm_map(myCorpus, replaceApostrophe)
inspect(myCorpus)
myCorpus <- tm_map(myCorpus, replaceApostrophe)
removeGarbage <- function(x) gsub("‰ÛÏ"," ",x)
inspect(myCorpus)
rm(list=ls()) ## just clears everything down to start
setwd("d:/Data/github/Grant") ## sets working directory, not really necessary, but makes things easier
# Needed <- c("tm", "SnowballC", "stringr")            ## These lines make sure that the packages
# install.packages(Needed, dependencies=TRUE)   ## are installed
library(tm)
#library(SnowballC)
#library(stringr)
tfile=read.csv(file.choose())  ## opens a file browser so can use other files than your fixed one
tfile=tfile[1] ## only want first column
tweet_corpus <- Corpus(VectorSource(tfile))
myCorpus <- tweet_corpus
removeURL <- function(x) gsub("http[[:alnum:][:punct:]]*", " ", x)
myCorpus <- tm_map(myCorpus, removeURL)
removeHandle <- function(x) gsub("@[[:alnum:][:punct:]]*"," ",x)
myCorpus <- tm_map(myCorpus, removeHandle)
removeHash <- function(x) gsub("#[[:alnum:][:punct:]]*"," ",x)
myCorpus <- tm_map(myCorpus, removeHash)
changeAmp <- function(x) gsub("&amp;"," ",x)
myCorpus <- tm_map(myCorpus, changeAmp)
replaceApostrophe <- function(x) gsub("‰Ûª","'",x)
myCorpus <- tm_map(myCorpus, replaceApostrophe)
removeGarbage <- function(x) gsub("‰Û_"," ",x)
myCorpus <- tm_map(myCorpus, removeGarbage)
removeGarbage <- function(x) gsub("‰ÛÏ"," ",x)
myCorpus <- tm_map(myCorpus, removeGarbage)
inspect(myCorpus)
myCorpus <- tm_map(myCorpus, tolower)
inspect(myCorpus)
myCorpus <- tm_map(myCorpus, removePunctuation, preserve_intra_word_dashes = TRUE)
inspect(myCorpus)
removeGarbage <- function(x) gsub("new pape"," ",x)
myCorpus <- tm_map(myCorpus, removeGarbage)
inspect(myCorpus)
rm(list=ls()) ## just clears everything down to start
rm(list=ls()) ## just clears everything down to start
setwd("d:/Data/github/Grant") ## sets working directory, not really necessary, but makes things easier
# Needed <- c("tm", "SnowballC", "stringr")            ## These lines make sure that the packages
# install.packages(Needed, dependencies=TRUE)   ## are installed
library(tm)
#library(SnowballC)
#library(stringr)
tfile=read.csv(file.choose())  ## opens a file browser so can use other files than your fixed one
tfile=tfile[1] ## only want first column
View(tfile)
View(tfile)
rm(list=ls()) ## just clears everything down to start
setwd("d:/Data/github/Grant") ## sets working directory, not really necessary, but makes things easier
# Needed <- c("tm", "SnowballC", "stringr")            ## These lines make sure that the packages
# install.packages(Needed, dependencies=TRUE)   ## are installed
library(tm)
#library(SnowballC)
#library(stringr)
tfile=read.csv(file.choose())  ## opens a file browser so can use other files than your fixed one
tfile=tfile[1] ## only want first column
tweet_corpus <- Corpus(VectorSource(tfile))
myCorpus <- tweet_corpus
removeGarbage <- function(x) gsub("‰Û[[:alnum:][:punct:]]*"," ",x)
myCorpus <- tm_map(myCorpus, removeGarbage)
inspect(myCorpus)
rm(list=ls()) ## just clears everything down to start
setwd("d:/Data/github/Grant") ## sets working directory, not really necessary, but makes things easier
# Needed <- c("tm", "SnowballC", "stringr")            ## These lines make sure that the packages
# install.packages(Needed, dependencies=TRUE)   ## are installed
library(tm)
#library(SnowballC)
#library(stringr)
tfile=read.csv(file.choose())  ## opens a file browser so can use other files than your fixed one
tfile=tfile[1] ## only want first column
tweet_corpus <- Corpus(VectorSource(tfile))
myCorpus <- tweet_corpus
removeGarbage <- function(x) gsub("‰Û[[:alnum:][:punct:]]*"," ",x)
myCorpus <- tm_map(myCorpus, removeGarbage)
inspect(myCorpus)
rm(list=ls()) ## just clears everything down to start
setwd("d:/Data/github/Grant") ## sets working directory, not really necessary, but makes things easier
# Needed <- c("tm", "SnowballC", "stringr")            ## These lines make sure that the packages
# install.packages(Needed, dependencies=TRUE)   ## are installed
library(tm)
#library(SnowballC)
#library(stringr)
tfile=read.csv(file.choose())  ## opens a file browser so can use other files than your fixed one
tfile=tfile[1] ## only want first column
Encoding(tfile) <- "latin1"
iconv(tfile, "latin1", "ASCII", sub="")
View(tfile)
rm(list=ls()) ## just clears everything down to start
setwd("d:/Data/github/Grant") ## sets working directory, not really necessary, but makes things easier
# Needed <- c("tm", "SnowballC", "stringr")            ## These lines make sure that the packages
# install.packages(Needed, dependencies=TRUE)   ## are installed
library(tm)
#library(SnowballC)
#library(stringr)
tfile=read.csv(file.choose())  ## opens a file browser so can use other files than your fixed one
tfile=tfile[1] ## only want first column
tfile <- sapply(tfile, function(row) iconv(row, "latin1", "ASCII", sub=""))
View(tfile)
View(tfile)
